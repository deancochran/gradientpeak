{
  "name": "gradientpeak",
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@playwright/test": "^1.58.2",
    "chokidar-cli": "^3.0.0",
    "lightningcss": "1.30.1",
    "turbo": "^2.6.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.20.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check": "biome check .",
    "dev": "turbo run dev --parallel --ui tui",
    "dev:web": "pnpm --filter web dev",
    "dev:mobile": "pnpm --filter mobile dev",
    "dev:apps": "turbo run dev --filter=!@repo/supabase --ui tui",
    "watch": "turbo run watch",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format --write .",
    "check-types": "turbo run check-types",
    "update-types": "pnpm --filter @repo/supabase update-types",
    "test": "turbo run test",
    "test:e2e:web": "pnpm --filter web test:e2e",
    "test:e2e:mobile": "pnpm --filter mobile test:e2e",
    "self-host:up": "pnpm --filter @repo/supabase dev:supabase",
    "self-host:down": "pnpm --filter @repo/supabase stop:supabase"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "pnpm": {
    "overrides": {
      "lightningcss": "1.30.1",
      "zod": "^4.2.1"
    }
  },
  "version": "1.0.0"
}
