{
  "name": "gradientpeak",
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "prettier": "^3.7.4",
    "turbo": "^2.6.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.20.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:apps": "turbo run dev --filter=!@repo/supabase",
    "dev:monitor:local": "node scripts/dev-monitor-local.mjs --clean",
    "watch": "turbo run watch",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "logs:scan": "node scripts/scan-dev-logs.mjs --since 10",
    "logs:scan:json": "node scripts/scan-dev-logs.mjs --since 10 --json",
    "self-host:up": "pnpm --filter @repo/supabase dev:supabase",
    "self-host:down": "pnpm --filter @repo/supabase stop:supabase"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "pnpm": {
    "overrides": {
      "zod": "^4.2.1"
    }
  },
  "version": "1.0.0"
}
