// Placeholder file - this tab redirects to /record
// This file exists only to make the tab bar button visible
// The actual navigation is handled in _layout.tsx

import { View } from "react-native";

export default function RecordLauncherPlaceholder() {
  // This component should never be rendered
  // The tabBarButton in _layout.tsx intercepts all presses
  return <View />;
}
