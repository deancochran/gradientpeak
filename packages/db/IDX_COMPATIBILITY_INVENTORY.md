# `idx` compatibility inventory

Repository-owned clients were searched across `apps/mobile`, `apps/web`, `packages/api`, and
`packages/core`. Neither app reads `idx` directly; their contracts are inferred from the API.
No external client contract exists in this source tree, so the compatibility conclusion is
limited to repository-owned clients.

| Table | Source consumer or contract | Semantics | Disposition |
| --- | --- | --- | --- |
| `profiles` | Profile base select/serialization | None; UUID is identity | Omit in new source/API; retain physically until contract |
| `activity_routes` | Route row schema and serializer fabricated `idx: 0` | None; UUID is identity | Omit in new source/API; retain physically until contract |
| `activity_plans` | Plan row schema, activity/event nesting, serializer fabricated `idx: 0` | None; UUID is identity | Omit in new source/API; retain physically until contract |
| `training_plans` | Inferred row contract and fixtures | None; UUID is identity | Omit in new source/API; retain physically until contract |
| `events` | Event repository selects and serializers fabricated `idx: 0` | None; UUID is identity | Omit in new source/API; retain physically until contract |
| `activities` | Inferred row/create contracts and fixtures | None; UUID is identity | Omit in new source/API; retain physically until contract |
| `integrations` | Integration API boundary and fixtures | None; UUID and provider/account keys identify rows | Omit in new source/API; retain physically until contract |
| `profile_metrics` | Latest-value queries in profile reads and profile updates | Substantive insertion-order tie-break when `recorded_at` is equal | Retain as the only `idx` exception |
| `oauth_states` | Inferred database contract only | None; UUID/state identify rows | Omit in new source/API; retain physically until contract |
| `integration_resource_links` | Inferred database contract only | None; UUID and internal/external unique keys identify rows | Omit in new source/API; retain physically until contract |
| `provider_sync_state` | Inferred database contract only | None; integration/resource key identifies rows | Omit in new source/API; retain physically until contract |
| `provider_sync_jobs` | Claim SQL and in-process lane ordering | Immutable queue insertion order | Add/backfill `queue_sequence bigint generated by default as identity`; retain physical `idx` until contract |
| `provider_webhook_receipts` | Inferred database contract only | None; UUID/provider event key identifies rows | Omit in new source/API; retain physically until contract |

The active migration is expand-compatible and drops no `idx` object. The guarded, non-auto-applied
`scripts/contract_redundant_idx_columns.sql` owns every eventual column, sequence, index, and
constraint drop. The upgrade fixture proves preserved provider sequence values, the next identity
value, normalized transitional fingerprint, guard rejection, and final contract fingerprint in a
rolled-back disposable transaction. Public contract tests prove omitted fields stay absent while
`queue_sequence` and the retained metric tie-breaker remain.
