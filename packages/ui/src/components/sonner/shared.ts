import type { CSSProperties } from "react";

export const TOASTER_STYLE_VARS = {
  "--normal-bg": "var(--popover)",
  "--normal-text": "var(--popover-foreground)",
  "--normal-border": "var(--border)",
} as CSSProperties;
