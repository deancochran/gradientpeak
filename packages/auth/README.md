# @repo/auth

This package is the long-term home for first-party auth ownership.

Current scope:

- normalized auth session contracts for web, mobile, and API consumers
- callback and deep-link contract definitions
- account deletion orchestration contracts
- runtime env parsing for the future Better Auth implementation

Out of scope for this first slice:

- provider integrations as login identity
- web framework route mounting
- direct API/router rewrites
