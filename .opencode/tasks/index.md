# OpenCode Task Index

Keep this file lean. It is startup context, so it should only track active or blocked work.

## Active

- Recording runtime flexibility design: active design/spec work now lives in `.opencode/specs/recording-runtime-flexibility/`. Current direction locks session class and core activity identity at start while allowing runtime plan/route guidance changes, with routes acting as live navigation for GPS-started sessions and virtual-course features for non-GPS-started sessions. Exact next action: lock the Phase 1 contract decisions for `sessionClass`, runtime guidance bindings/events, and trainer automatic-authority semantics before implementation begins.
- Provider onboarding/import policy research: completed an official-docs-first pass across Garmin, Wahoo, TrainingPeaks, Strava, Polar, Fitbit, and Apple Health for cold-start mitigation. Current truth: Wahoo and Fitbit look strongest for user-consented profile-prefill plus historical import; Strava is technically rich but contractually poor for durable retained anti-cold-start storage; Garmin and TrainingPeaks require approval and still have gated/unclear retention terms that should be treated as partnership/legal blockers; Apple Health is mobile/on-device only; Polar is too limited for deep backfill. Exact next action: turn this into a provider-by-provider policy matrix plus a product/legal checklist before implementation.
- Mobile E2E stabilization: active coordination now lives in `.opencode/specs/mobile-e2e-stabilization/`. Current branch now batch-ran 62 non-reusable Maestro flows one by one, fixed stale fixture defaults plus a `goal_entry_open` YAML parse error, and hardened reusable login recovery from sign-up/forgot-password screens. Present blocker: local auth-backed flows still stop at `The authentication service is not responding` until the running web auth process is restarted with a reachable `DATABASE_URL`/`POSTGRES_URL`/`SUPABASE_DB_URL` and the target DB is healthy. The hidden Better Auth-only fallback to `127.0.0.1:54322` has been removed, so config mismatch is no longer masking the real env/runtime state.
- Verified local auth DB target: `supabase status` reports the running local database URL as `postgresql://postgres:postgres@127.0.0.1:54322/postgres`, while `apps/web/.env.local` currently sets only `APP_URL`/`NEXT_PUBLIC_APP_URL` and no DB URL. Exact next action: add that DB URL to `apps/web/.env.local` (or export it before starting web), then restart the web dev server.
- Follow-up verification: the exact `DATABASE_URL` value was added to `apps/web/.env.local`. The remaining failure was not a config typo but a broken local Supabase stack: `supabase status` claimed DB health while `127.0.0.1:54322` was not actually listening. After removing the stale `supabase_auth_gradientpeak` container and restarting Supabase, ports `54321` and `54322` are now listening and a raw TCP probe to `127.0.0.1:54322` succeeds. Exact next action: retry mobile sign-in against the already-running web server; restart web only if it still holds a stale DB connection error.
- Root DB workflow improved for local use: `package.json` now exposes simple root-level Supabase migration commands (`pnpm db:update`, `pnpm db:downgrade`, `pnpm db:reset`, `pnpm db:migration:new`, `pnpm db:migration:list`, `pnpm db:diff`, `pnpm db:lint`, `pnpm db:status`) plus Drizzle inspection helpers (`pnpm db:drizzle:pull`, `pnpm db:drizzle:studio`). Note: Drizzle `push` was intentionally not kept as a root shortcut because this repo currently triggers an interactive conflict prompt in non-TTY shells; local schema state should be managed through the Supabase migration commands for now.
- Better Auth local user mismatch fixed: `packages/supabase/migrations/20260403155000_better_auth_public_tables.sql` now creates/backfills Better Auth public tables from `auth.users`, and `packages/auth/src/runtime/server.ts` now uses bcrypt-compatible password verification so existing Supabase credential hashes can authenticate. Applied locally via `pnpm db:update`; verified that `charles.dean.cochran@outlook.com` now exists in `public.users` with a `credential` account row. Exact next action: retry sign-in after the running web dev server reloads/restarts onto the new auth code.
- Mobile authenticated-401 follow-up: the mobile app was marking itself authenticated but tRPC requests only attached a bearer token when `session.bearerToken` existed. The Expo Better Auth client actually persists session cookies in secure storage and exposes them via `authClient.getCookie()`, so tRPC was sending neither bearer auth nor cookie auth in the common mobile path. `apps/mobile/lib/auth/auth-headers.ts` now forwards the stored Better Auth cookie header (and still sends bearer auth when present). Exact next action: reload the mobile bundle and retry `profiles.get`; if it still 401s, inspect whether the stored Better Auth cookie is empty after sign-in.
- `profiles.get` 500 root cause found: web tRPC routes use `@repo/db/client`, which currently relies on `@vercel/postgres` and requires `POSTGRES_URL`, not just `DATABASE_URL`. Auth sign-in succeeded because Better Auth uses the shared `DATABASE_URL`, but the API DB client failed with `missing_connection_string`. Added `POSTGRES_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres` to `apps/web/.env.local`. Exact next action: restart the web dev server, then retry loading the account.
- `profiles.get` 500 follow-up root cause found: even with `POSTGRES_URL`, `@vercel/postgres` rejects the local direct Supabase string as `invalid_connection_string` because it expects a pooled Vercel-style URL. Fixed by switching `packages/db/src/client.ts` to Drizzle's stable `node-postgres` driver backed by `pg` and the shared DB URL resolver. Focused `@repo/db`, `@repo/auth`, and `web` typechecks pass, and a direct Drizzle query against local `profiles` now succeeds. Exact next action: restart the web dev server and retry account loading.
- Latest `profiles.get` 500 root cause found and fixed: the actual local `public.profiles` table was missing the newer `email` and `full_name` columns that the current Drizzle schema/router selects. Added Supabase migration `20260403170500_profiles_auth_columns.sql`, applied it with `pnpm db:update`, and verified `profilesRouter.createCaller(...).get()` now succeeds for `d04f152d-cd83-4e56-8d34-db1fdc2c69e7`. Exact next action: restart the web dev server if it has not been restarted since the DB driver change, then retry account loading in mobile.
- Better Auth signup/profile provisioning path is now fixed instead of relying on the retired `auth.users` trigger path. `packages/auth/src/runtime/server.ts` now generates UUID ids for Better Auth records, `packages/db/src/auth-schema.ts` models auth user ids as UUID, and new Supabase migrations (`20260403173500_better_auth_profile_provisioning.sql`, `20260403174500_better_auth_uuid_user_ids.sql`, `20260403175500_fix_better_auth_profile_trigger_uuid.sql`) rewire `public.profiles.id` to `public.users(id)` and install `public.users` insert/update triggers that provision/sync `public.profiles`. Verified by inserting a temporary Better Auth user and observing automatic profile creation, by updating a real Better Auth user's name and observing profile sync, and by re-running `profilesRouter.createCaller(...).get()` successfully. Exact next action: restart the running web dev server and retry the mobile/web sign-up or account load flow against the refreshed server.
- API endpoint-test program started: one delegated audit agent per top-level router has inventoried endpoint coverage gaps, and a shared `@repo/api` Vitest harness now exists (`packages/api/vitest.config.ts`, `packages/api/src/test/setup.ts`, `packages/api/src/test/db-module.ts`) so router suites no longer fail immediately on `@repo/db` import-time env setup. Current truth: many router test files are still stale against the Drizzle/`ctx.db` architecture, so the next execution wave is endpoint-by-endpoint test implementation and modernization per router, not just turning on `pnpm --filter @repo/api test`.
- `@repo/api` coverage audit refresh: router-level coverage is structurally broad, but package-wide coverage is not yet full. Current verification on the primary checkout: `pnpm --filter @repo/api test` fails in 7 files / 56 tests, mostly the training-plan + planned-activities cluster with `TRPCError: Database client unavailable`; `src/lib/activity-analysis/__tests__/derived.test.ts` also fails with `RangeError: Invalid time value`; and `pnpm --filter @repo/api exec vitest run --coverage` is currently blocked because `@vitest/coverage-v8` is not installed. Exact next action: add the Vitest coverage provider, then fix the failing `ctx.db` training-plan harness path and the invalid-date activity-analysis fixture before rerunning package coverage.
- `@repo/api` test harness follow-up landed: `@vitest/coverage-v8@4.0.18` is now installed for `packages/api`, stale training-plan/planned-activities suites now use a shared Drizzle-style query-map DB mock (`packages/api/src/test/mock-query-db.ts`), and the derived activity-analysis assertions are aligned with the current string `computed_as_of` contract. Verification now passes with `pnpm --filter @repo/api test` and `pnpm --filter @repo/api exec vitest run --coverage`. Current truth: this is not yet full package coverage — the latest Vitest/V8 report is `64.16%` statements / `45.71%` branches / `69.3%` functions / `64.89%` lines, with the biggest remaining gaps in `src/lib/integrations/wahoo/*`, `src/infrastructure/repositories/*`, `src/utils/{estimation-helpers,planning-helpers}.ts`, and parts of `src/routers/planning/training-plans/base.ts`. Exact next action: prioritize direct tests for the highest-risk untested helper/integration/repository modules instead of adding more router-level mock coverage.
- `@repo/api` coverage wave 2 started with direct helper coverage instead of more router mocks: new suite `packages/api/src/utils/__tests__/estimation-helpers.test.ts` now exercises `addEstimationToPlan`, `addEstimationToPlans`, and `computePlanMetrics` across store-backed vs legacy readers, route dedupe, null-plan skipping, route-backed distance propagation, and per-plan fallback behavior when estimation fails. Verification passes with both `pnpm --filter @repo/api exec vitest run src/utils/__tests__/estimation-helpers.test.ts` and a full `pnpm --filter @repo/api exec vitest run --coverage`. Current truth: package coverage improved to `65.18%` statements / `46.92%` branches / `70.89%` functions / `65.9%` lines. Exact next action: continue the highest-risk wave with direct tests for `packages/api/src/infrastructure/repositories/drizzle-training-plan-repository.ts` or the Wahoo integration helpers, since those remain the largest untested risk seams.
- `@repo/api` coverage wave 3 landed on the real training-plan repository seam: new suite `packages/api/src/infrastructure/repositories/__tests__/drizzle-training-plan-repository.test.ts` covers create/update success + missing-return failures, owned-plan lookup, prior inferred snapshot resolution (explicit and active-future fallback), invalid/missing metadata null behavior, and snapshot persistence with metadata preservation/no-op semantics. An unrelated stale assertion in `src/routers/__tests__/events.test.ts` was also updated to match the current `rest_day` validation message so full coverage could run green again. Verification now passes with `pnpm --filter @repo/api exec vitest run src/infrastructure/repositories/__tests__/drizzle-training-plan-repository.test.ts` and a full `pnpm --filter @repo/api exec vitest run --coverage`. Current truth: package coverage improved again to `65.52%` statements / `47.35%` branches / `71.09%` functions / `66.26%` lines. Exact next action: attack the largest remaining high-risk blind spot with direct tests for `packages/api/src/lib/integrations/wahoo/{sync-service,activity-importer,plan-converter,route-converter}.ts`.
- `@repo/api` Wahoo coverage wave started with the importer seam: new suite `packages/api/src/lib/integrations/wahoo/activity-importer.test.ts` now directly covers missing integration, duplicate short-circuiting, happy-path import with linked planned event + FIT upload, non-fatal FIT upload failure, unknown/missing workout type fallback to `other`, and deterministic started/finished timestamp fallback behavior. Verification passes with `pnpm --filter @repo/api exec vitest run src/lib/integrations/wahoo/activity-importer.test.ts` and a full `pnpm --filter @repo/api exec vitest run --coverage`. Current truth: package coverage improved to `65.91%` statements / `47.83%` branches / `71.6%` functions / `66.64%` lines, and Wahoo package coverage moved off near-zero (`activity-importer.ts` now ~85% lines). Exact next action: continue the Wahoo wave with direct tests for `packages/api/src/lib/integrations/wahoo/plan-converter.ts` or `sync-service.ts`, with `plan-converter.ts` the smaller next branch-coverage win and `sync-service.ts` the larger risk seam.
- Wahoo coverage wave expanded across both converter and orchestration seams: new suites `packages/api/src/lib/integrations/wahoo/plan-converter.test.ts` and `packages/api/src/lib/integrations/wahoo/sync-service.test.ts` now cover workout duration math, repeated interval conversion, compatibility warnings/incompatibility branches, new sync creation with route handling, metadata-only update, recreate-on-newer-plan, and unsync cleanup even when remote delete fails. Verification passes with `pnpm --filter @repo/api exec vitest run src/lib/integrations/wahoo/plan-converter.test.ts src/lib/integrations/wahoo/sync-service.test.ts` and a full `pnpm --filter @repo/api exec vitest run --coverage`. Current truth: package coverage improved materially to `69.49%` statements / `50.68%` branches / `73.92%` functions / `70.33%` lines; Wahoo package coverage is now `58.19%` statements / `52.39%` branches / `55%` functions / `59.28%` lines, with biggest remaining Wahoo gaps in `route-converter.ts`, `client.ts`, and some uncovered sync-service branches. Exact next action: add direct tests for `packages/api/src/lib/integrations/wahoo/route-converter.ts`, then decide whether to push further into `client.ts` or shift back to the lowest-covered repository seams.
- Wahoo route coverage is now closed as a pure-logic seam: new suite `packages/api/src/lib/integrations/wahoo/route-converter.test.ts` covers GPX base64 conversion, start-coordinate extraction, missing-trackpoint null handling, supported/unsupported activity validation, distance warning branches, and workout-family mapping. Verification passes with `pnpm --filter @repo/api exec vitest run src/lib/integrations/wahoo/route-converter.test.ts` and a full `pnpm --filter @repo/api exec vitest run --coverage`. Current truth: package coverage improved again to `69.98%` statements / `51.13%` branches / `74.58%` functions / `70.83%` lines; Wahoo package coverage is now `64.2%` statements / `58.9%` branches / `65%` functions / `65.47%` lines, with the dominant remaining Wahoo blind spot now concentrated in `client.ts` plus a few uncovered `sync-service.ts` / `activity-type-utils.ts` branches. Exact next action: decide whether to directly test `packages/api/src/lib/integrations/wahoo/client.ts` with fetch-boundary mocks or pivot back to the lowest-covered repository/helper seams outside Wahoo.
- Wahoo client fetch-boundary coverage is now in place: new suite `packages/api/src/lib/integrations/wahoo/client.test.ts` covers plan/workout request shaping, 204 delete handling, query encoding, structured JSON API errors, non-JSON HTTP errors, fetch failures, and `withRetry` retry/backoff behavior, all without production-code changes. Verification passes with `pnpm --filter @repo/api exec vitest run src/lib/integrations/wahoo/client.test.ts` and a full `pnpm --filter @repo/api exec vitest run --coverage`. Current truth after the latest full rerun: package coverage is now `71.26%` statements / `51.64%` branches / `75.8%` functions / `72.13%` lines, and Wahoo package coverage is `80.13%` statements / `66.43%` branches / `83.33%` functions / `81.42%` lines. The dominant remaining broad gap is no longer Wahoo conversion/client code but the untested Drizzle repository seams, especially `src/infrastructure/repositories/drizzle-event-{read,write,completion}-repository.ts`, followed by `drizzle-wahoo-repository.ts` and integration/calendar persistence seams. Exact next action: add a direct suite for `packages/api/src/infrastructure/repositories/__tests__/drizzle-event-read-repository.test.ts` covering estimation inputs, accessible training-plan projection lookup, list filters/cursor behavior, and ISO/null serialization.
- Event repository coverage wave started on the read seam: new suite `packages/api/src/infrastructure/repositories/__tests__/drizzle-event-read-repository.test.ts` now covers estimation-input aggregation, accessible training-plan projection lookup, owned-event SQL filter/cursor construction, and ISO/null serialization. Verification passes with `pnpm --filter @repo/api exec vitest run src/infrastructure/repositories/__tests__/drizzle-event-read-repository.test.ts` and a full `pnpm --filter @repo/api exec vitest run --coverage`. Current truth: package coverage improved again to `71.68%` statements / `52.25%` branches / `76.35%` functions / `72.56%` lines, and `src/infrastructure/repositories/*` moved up to `39.39%` statements / `37.19%` branches / `26.31%` functions / `41.39%` lines, with `drizzle-event-read-repository.ts` itself now around `64.15%` statements / `53.22%` branches / `45.83%` functions / `66%` lines. Exact next action: continue this repository wave with direct tests for `packages/api/src/infrastructure/repositories/drizzle-event-write-repository.ts`, then `drizzle-event-completion-repository.ts`.
- Wahoo client fetch-boundary coverage is now in: new suite `packages/api/src/lib/integrations/wahoo/client.test.ts` directly covers plan/workout request shaping, 204 delete handling, encoded route-list queries, structured JSON API errors, non-JSON HTTP errors, wrapped fetch failures, and `withRetry` retry/non-retry branches. Focused verification passed with `pnpm --filter @repo/api exec vitest run src/lib/integrations/wahoo/client.test.ts`. Exact next action: rerun full `@repo/api` coverage to measure the Wahoo lift, then choose between finishing the remaining small `sync-service.ts` / `activity-type-utils.ts` branches or shifting back to the next lowest-covered non-Wahoo seam.
- `@repo/api` Wahoo coverage wave reached the remaining route helper seam: new direct suite `packages/api/src/lib/integrations/wahoo/route-converter.test.ts` now covers GPX base64 preparation, first-trackpoint coordinate extraction, missing-trackpoint null handling, unsupported-route validation errors with short-distance warnings, long-route warnings, and workout-family mapping defaults. Focused verification passed with `pnpm --filter @repo/api exec vitest run src/lib/integrations/wahoo/route-converter.test.ts`. Exact next action: rerun full `@repo/api` coverage to measure the Wahoo lift, then decide whether to push into `client.ts` or shift back to the lowest-covered repository/helper seams.
- `@repo/api` coverage wave 2 follow-up: direct repository-seam tests now cover `packages/api/src/infrastructure/repositories/drizzle-training-plan-repository.ts` across create/get/update behavior, active future-plan snapshot fallback, invalid/missing snapshot metadata returning `null`, explicit plan-id snapshot persistence with metadata preservation, no-op persistence when no target plan resolves, and `TRPC BAD_REQUEST` when create/update return no row. Verification passed with `pnpm --filter @repo/api exec vitest run src/infrastructure/repositories/__tests__/drizzle-training-plan-repository.test.ts`. Exact next action: rerun package coverage to measure the new seam coverage and then continue with the next highest-risk gap, likely the Wahoo integration helpers.
- Mobile E2E stable-build design: follow-up research and planning now lives in `.opencode/specs/mobile-e2e-stable-build/`. Current local stabilization work stayed on Expo Dev Client per Maestro blog guidance: added a reusable Expo setup flow plus a simpler `apps/mobile/scripts/prepare-maestro-device.sh` that launches the dev client, runs the setup flow, and verifies app content visibility. Next step is rerunning `pnpm run test:e2e` from the localhost `dev:e2e` loop and debugging any remaining in-app flow failures from that cleaner baseline.
- Mobile E2E stable-build design: follow-up research and planning now lives in `.opencode/specs/mobile-e2e-stable-build/`. Current local stabilization work stayed on Expo Dev Client per Maestro blog guidance: added a reusable Expo setup flow plus a simpler `apps/mobile/scripts/prepare-maestro-device.sh` that launches the dev client, runs the setup flow, and verifies app content visibility. Also added a reusable neutral-start flow at `apps/mobile/.maestro/flows/reusable/reset_to_home.yaml` so authenticated flows can always begin from Home before navigating to a target tab. Focused `tabs_smoke` now reaches login, Home reset, Discover, and Plan successfully; the remaining failure is the Calendar leg where `create-event-entry` does not become visible after tapping `Calendar` in `apps/mobile/.maestro/flows/reusable/open_calendar_tab.yaml`.
- Mobile calendar dual-mode redesign: specification is now captured in `.opencode/specs/calendar-dual-mode-redesign/`. The active shell is simpler now too: `apps/mobile/components/calendar/CalendarHeader.tsx` keeps the day/month toggle left-aligned, the create button right-aligned, and shows a month-only reset-to-day button instead of the old extra chrome; `CalendarDayList.tsx` also drops the duplicate day-count card so day content starts more directly under the header. Rest-day migration is now in across mobile/core/API: calendar actions keep only planned activity / race target / custom creation, the mobile manual-create flow no longer accepts `rest_day`, the day surface infers rest from dates without `planned` events, `packages/core` no longer materializes `rest_day` plan events or accepts them in editable write schemas, and `packages/api/src/routers/events.ts` blocks new `rest_day` writes while hiding legacy `rest_day` rows from router reads. Phase 3 sheet/detail follow-up is now in too: home-card string-matching rest logic is gone, day taps stay sheet-first, routed detail/edit is fallback-only for supported planned/custom/race-target paths, imported plus legacy `rest_day` preview states stay read-only with no detail/edit affordances, and the routed `event-detail` screen now presents itself as the advanced fallback behind the preview sheet while preserving planned-event reschedule handoff through `ScheduleActivityModal`. Verification passed with `pnpm --filter mobile check-types`, `pnpm --filter mobile exec vitest run lib/calendar/__tests__/eventRouting.test.ts lib/calendar/__tests__/eventPresentation.test.ts`, `pnpm --filter mobile test:jest:file calendar-screen.jest.test.tsx event-detail-delete-redirect.jest.test.tsx event-detail-fallback.jest.test.tsx`, `pnpm --filter mobile test:jest:file home-cards.jest.test.tsx`, `pnpm --filter @repo/core check-types`, targeted core Vitest, `pnpm --filter @repo/api exec vitest run src/routers/__tests__/events.test.ts`, and `pnpm --filter @repo/api check-types`. Exact next action: do a device/simulator runtime pass on calendar preview/detail affordances and card density, then only continue remaining Phase 4 or polish work if that runtime pass surfaces issues.
- TanStack Start / Drizzle / Better Auth replatform: active implementation is now underway in `.opencode/specs/tanstack-start-drizzle-auth-replatform/`, using `t3-oss/create-t3-turbo` as the reference architecture. Current delivered work removed shared `@repo/supabase` contract ownership, moved DB-owned seed scripts into `packages/db/scripts/`, mechanically shrank API bridge generics away from `DbSupabaseDatabase` inside `packages/api`, added the Better Auth Next handler in `apps/web/src/app/api/auth/[...all]/route.ts`, cut the first web auth slice over to the Better Auth client, completed `integrations`/`oauth_states` schema parity in `packages/db/src/schema/tables.ts`, and moved `list`/`disconnect`/`getAuthUrl`/`refreshToken`/`cleanupExpiredStates`/`validateOAuthState`/`storeIntegration`/`deleteOAuthState` onto the Drizzle repository seam. The settings-page delete-account action is intentionally gated, the confirm route no longer executes the legacy Supabase OTP verification flow, and mobile Lane H has advanced far enough that `packages/api/src/routers/auth.ts` is deleted entirely: external mobile auth screens use the Better Auth mobile client seam, signed-in mobile `deleteAccount`, `updatePassword`, and `updateEmail` use local helpers/Better Auth flows, and auth mail delivery now runs through a shared Nodemailer mailer in `packages/auth/src/runtime/mailer.ts` with env-driven SMTP/log/disabled modes. Mailtrap can now be used as the development SMTP target via the new envs in `apps/web/.env.example` and `apps/web/selfhost.env.example`. Architecture alignment follow-up is now also underway: `.opencode/specs/tanstack-start-drizzle-auth-replatform/repo-boundaries.md` captures target package boundaries, the next wave of mobile recording-category and submission-adjacent files has moved off `@repo/db`, and the activity submission draft is now camelCase/mobile-local instead of DB-shaped. The web integrations callback route no longer injects a stateless Supabase service-role client, the dead browser Supabase helper `apps/web/src/lib/supabase/client.ts` is deleted, the unused `createStatelessServiceRoleClient` helper is removed, the obsolete Supabase session-refresh middleware bridge (`apps/web/src/middleware.ts` and `apps/web/src/lib/supabase/middlewear.ts`) is now deleted, `apps/web/src/lib/api/server.tsx` no longer creates an anon/publishable Supabase SSR client, and `apps/web/src/lib/supabase/server.ts` is reduced to the temporary service-role helper needed by `/api/trpc`. The first low-risk `ctx.supabase` API slice is now landed too: `packages/api/src/lib/integrations/ical/sync-service.ts` uses a Drizzle-backed repository and `packages/api/src/routers/integrations.ts` no longer constructs iCal sync with `ctx.supabase`. Wahoo seam preparation is now active too, with `packages/api/src/repositories/wahoo-repository.ts` and `packages/api/src/infrastructure/repositories/drizzle-wahoo-repository.ts` in place as the first DB-state seam, while storage remains intentionally deferred to Supabase platform adapters. `pnpm --filter @repo/auth check-types`, `pnpm --filter @repo/api check-types`, `pnpm --filter mobile check-types`, `pnpm --filter web check-types`, and `pnpm --filter @repo/ui check-types` all pass. Next step is wiring that Wahoo repository into `WahooSyncService` / `WahooActivityImporter` and updating the integrations/events/router call sites before attempting to retire the `/api/trpc` service-role Supabase injection.
- Merge-readiness validation sweep: root `pnpm check-types`, `pnpm lint`, and `pnpm test` now pass on this branch. The only blocker found during the first pass was root Biome traversing nested `.worktrees/*/biome.json` files and failing on duplicate-root config detection; the root `biome.json` now excludes `.worktrees/**`. Exact next action: do final human review and commit when ready; incomplete feature/spec work remains tracked in the active specs.
- Worktree consolidation audit: `wt list` currently shows 36 worktrees. Only 4 are immediately safe to remove because they are clean and exactly aligned with the current branch head (`spec/tanstack-start-drizzle-auth-replatform/web-bridge-cleanup-local`, `.../web-auth-forms`, `.../web-route-hardening`, `.../web-settings-form`). Several `*-local` web worktrees are same-head but still have uncommitted file changes, and most remaining mobile/replatform worktrees are stale divergent branches (6-8 commits ahead of their own line, 43 behind the current branch) with some clean and many dirty states. Exact next action: perform branch-by-branch consolidation decisions (keep/merge vs discard) before any commit, push, or pruning/removal.
- Web hardening follow-up under `.opencode/specs/tanstack-start-drizzle-auth-replatform/`: second-pass cleanup landed too. Header/account chrome is more coherent, message counts are live, followers/following now append paginated results, the stale `apps/web/src/lib/supabase/server.ts` bridge is removed, coaching dead links are cleaned up, and `/dev/ui-preview` is blocked in production with `noindex`. Next step: manually verify the new web auth/nav/pagination/coaching flows in-browser and then fix any polish regressions that surface.
- Mobile stack research follow-up: package-level review plus doc research completed for Expo 54, Expo Router 6, React Hook Form, Zod, Drizzle-backed contracts, React Query, SecureStore, NativeWind, and Expo operational tooling. Recommended defaults are now clear: Expo Router for routing only, thin route files with feature modules outside `app/`, React Query for server state, Zustand only for small client/UI state, RHF + shared app-facing Zod schemas for forms, Drizzle kept server-only, and `@repo/api`/`@repo/core` as the mobile boundary layers. Next step: capture these defaults in a short mobile architecture decision record before auditing `apps/mobile` against them.
- Mobile stack hardening: a full `apps/mobile` audit plus another implementation pass landed. Smaller off-pattern forms (`route-upload`, `ProfileSection`) now use RHF + Zod + shared wrappers, `ScheduleActivityModal` has a simpler submit/error path, external onboarding is fenced back into the canonical guarded flow, and focused external auth/runtime Jest coverage now covers sign-up, sign-in, forgot-password, reset-password, callback, onboarding redirect, root layout, and schedule modal. Training preferences now also use RHF + Zod + shared form wrappers with a core-owned baseline-date form adapter so draft preview, reset, and save stay aligned with persisted settings. Wave 2 on `plan.tsx` advanced again: after the earlier `usePlanGoalEditorController.ts` extraction, the memo-heavy read-only dashboard derivations now live in `apps/mobile/components/plan/usePlanDashboardViewModel.ts`, so the tab route keeps query ownership, refresh choreography, router actions, and modal wiring while shedding the view-model shaping block. The next training-plan composer slices also landed: plan metadata is now a dedicated RHF + shared-wrapper section, save uses validated metadata values instead of the old ad hoc empty-name guard, the inline goal target editor modal moved out of `SinglePageForm.tsx`, the active goal targets card/list now lives in local `GoalTargetsSection` ownership with focused Jest coverage, the Limits tab now routes editable CTL/recovery fields through a bounded RHF-owned `ConstraintsConfigSection`, the Tuning tab now routes `behaviorControlsV1` through `BehaviorControlsConfigSection` with corrected reset wiring, and the Availability tab now routes `planStartDate` plus weekday toggles through `AvailabilityConfigSection` while preserving the parent reset path and avoiding false user-provenance flips on date-only edits. `TrainingPlanComposerScreen.metadata.jest.test.tsx` now covers the user-visible mount-time metadata validation path through the real rendered Plan tab (`Plan name is required.` and `Needs attention: Plan`). A conservative dependency refresh wave also landed cleanly (`react-native-ble-plx`, `superjson`, `victory-native`, `zustand`, `@swc/core`, `@types/lodash`), while attempted RHF/React Query/type-package jumps were rolled back after exposing current repo compatibility gaps. `activity-plan-detail.tsx` is now a thin route boundary that delegates to `apps/mobile/components/activity-plan/ActivityPlanDetailScreen.tsx`, scheduling/duplicate orchestration now lives in `apps/mobile/components/activity-plan/useActivityPlanSchedulingActions.ts`, social/comment orchestration now lives in `apps/mobile/components/activity-plan/useActivityPlanSocialController.ts`, the remaining derived detail shaping now lives in `apps/mobile/components/activity-plan/useActivityPlanDetailViewModel.ts`, and the summary header card now lives in the pure presentational `apps/mobile/components/activity-plan/ActivityPlanSummarySection.tsx` with focused route/scheduling/social/view-model Jest coverage still passing. The next route-sized hotspot is also thinning: `apps/mobile/app/(internal)/(standard)/training-plan-detail.tsx` no longer renders its full structure/linked-activity-plan section inline; that UI now lives in `apps/mobile/components/training-plan/TrainingPlanStructureSection.tsx` with regression coverage for linked-plan non-finite metric fallback and route-owned picker close policy, schedule/apply-template orchestration now lives in `apps/mobile/components/training-plan/useTrainingPlanTemplateSchedulingController.ts` plus `TrainingPlanTemplateSchedulingDialog.tsx` with focused deeplink coverage for anchor selection, finish-date validation, active-plan concurrency warning gating, Open Current Plan routing, and success-path scheduling actions, the summary header/snapshot/actions block now lives in the pure presentational `apps/mobile/components/training-plan/TrainingPlanDetailHeaderActionsSection.tsx`, and like/privacy/duplicate ownership now lives in `apps/mobile/components/training-plan/useTrainingPlanHeaderSocialActions.ts` with focused hook/component/deeplink Jest coverage for invalid-ID like guards, optimistic like mutation, privacy toggles, duplicate CTA rendering, and scheduling confirm flow. Next step: continue thinning `training-plan-detail.tsx` with another bounded seam (likely danger-zone extraction or the remaining no-plan empty state / focus banner/header route assembly), or return to another remaining large route once this screen reaches a good stopping point.
- Drizzle alignment follow-up: internet-backed review confirmed the latest stable line is `drizzle-orm@0.45.2` / `drizzle-kit@0.31.10`, so the repo is now back on stable Drizzle syntax instead of the beta detour. `packages/db`, `packages/auth`, and `packages/api` now align on stable ORM usage; relation imports are back to `drizzle-orm`, DB schema validation uses `drizzle-zod@0.8.3`, and focused `@repo/db` / `@repo/auth` / `@repo/api` typechecks all pass. Next step: resume the auth runtime/env cleanup knowing the Drizzle package surface is no longer the blocker.
- Training-plan-detail cleanup is effectively complete for this pass: focus banner and no-plan empty state now live in `TrainingPlanDetailFocusBanner.tsx` and `TrainingPlanNoPlanEmptyState.tsx`, while route-owned focus-context derivation, refresh handling, and create CTA wiring remain intact. Next route-sized hotspot to tackle is still `apps/mobile/app/(internal)/(standard)/training-preferences.tsx` (~788 lines).
- Training-preferences cleanup has started: the projection preview cluster (active-plan query, snapshot hook, projection derivation, preview state, and preview card) now lives in `apps/mobile/components/settings/TrainingPreferencesProjectionPreview.tsx`, and the focused preview Jest suite still passes. Remaining worthwhile seams in `training-preferences.tsx` are now the tab panels and summary/footer assembly; next best bounded move is extracting one or more tab sections into pure presentational components while keeping form ownership in the route.
- Mobile boundary cleanup Wave 1: profile avatar upload, manual historical FIT upload, and FIT file download helper paths now use `@repo/api` signed URL seams instead of direct mobile Supabase/storage access. Follow-up boundary cleanup also removed live `apps/mobile` references to `@repo/db`, deleted the unused mobile Supabase client bridge, and moved bearer-token request headers into `apps/mobile/lib/auth/auth-headers.ts`. `apps/mobile/lib/hooks/useAuth.ts` no longer calls the Better Auth client directly for delete-account or password-update flows; those now route through `apps/mobile/lib/auth/account-management.ts`, the hook preserves optimistic onboarding/profile state over stale query data, and focused auth tests now cover both the adapter delegation and the optimistic precedence behavior. Remaining blockers in this lane: `packages/db` still cannot migrate from `drizzle-zod` to `drizzle-orm/zod` without a broader Drizzle upgrade.
- tRPC API centralization audit: finalized the structural pass by normalizing remaining mixed router filenames to kebab-case (`activity-plans.ts`, `activity-efforts.ts`, `profile-settings.ts`), removing the old flat training-plan/profile-access shims, and updating grouped imports/tests accordingly. Broader verification passed for `@repo/trpc`, web, and mobile typechecks, and targeted trpc router tests passed. Mobile test-harness follow-up also landed: `apps/mobile/vitest.config.ts` now excludes Jest-owned suites and resolves `@repo/core/*` subpaths correctly, so targeted Vitest runs no longer explode on JSX/parser alias errors. Targeted Jest runs for `test/jest-smoke.jest.test.tsx` and `components/training-plan/create/__tests__/SinglePageForm.blockers.jest.test.tsx` also pass; the remaining noise is mostly benign `react-test-renderer` deprecation warnings rather than harness breakage.
- Historical activity imports: the FIT-first manual import spec in `.opencode/specs/historical-activity-imports/` is now functionally complete. `packages/core/activity-analysis/*` and `packages/trpc/src/lib/activity-analysis/*` own the dynamic, time-causal derived model; `activities.getById`/`list`/`listPaginated`, mobile detail/list/feed surfaces, and `home`/`trends` all consume dynamic derived values instead of the removed stale activity columns; `fit-files.processFitFile` stamps imported `activity_efforts`/`profile_metrics` at historical completion time; and the final provenance/testing gap is closed via explicit `activities.import_*` fields (`import_source`, `import_file_type`, `import_original_file_name`), a Supabase migration plus regenerated types, focused mobile Jest coverage for `integrations.tsx`, and a derived-analysis regression proving later reads incorporate older backfilled history. Remaining items in the spec are conditional follow-up docs (`project-reference.md` and broader release docs) rather than implementation blockers.
- Cross-platform UI testing migration: `sheet` now has fixture/story/play coverage, `separator`, `table`, and `toggle` have story ownership, browser Storybook coverage is up to 41 suites / 73 tests, and the shared preview registry includes a composite `formFields` scenario. The remaining overlay/navigation primitives were audited for real shared usage: `navigation-menu` is the only web-only story candidate if product usage emerges; `popover`, `hover-card`, `context-menu`, and `menubar` are currently package-test-only because they are not shared across web/mobile product surfaces. Next step is deciding whether to add a dedicated web-only `navigation-menu` story now or keep it deferred until product usage appears.

- Wahoo importer/storage seam audit: reviewed `packages/api/src/lib/integrations/wahoo/activity-importer.ts`, `apps/web/src/app/api/webhooks/wahoo/route.ts`, and nearby Wahoo call sites. Safe-next split is to move importer DB reads/writes behind API-owned repositories while keeping Supabase storage upload as an explicit platform adapter for now; broader Wahoo sync-service and webhook-hosting cleanup remains deferred until the `synced_events` and route-storage seams are designed.

## Archive

- Dev startup ergonomics: aligned root `pnpm dev` with raw `turbo run dev`, switched web's concurrent subcommands from `npm run` to `pnpm run` to remove workspace config warning noise, removed the infra-only `@repo/supabase` package from the monorepo-wide `dev` task so missing Docker no longer crashes the default app dev loop, trimmed redundant root aliases (`dev:apps`, `self-host`, `self-host:stop`) so the root script surface is smaller, pinned mobile Metro host selection via `REACT_NATIVE_PACKAGER_HOSTNAME=100.119.109.24` in `apps/mobile/.env.local` so Expo advertises the Tailscale address instead of a `192.*` LAN IP, fixed mobile Better Auth Expo storage wiring to use sync `expo-secure-store` methods (`getItem`/`setItem`) so sign-up no longer throws `storage.getItem is not a function`, repaired Better Auth Drizzle schema exports so the adapter can see `users`/`sessions`/`accounts`/`verifications` instead of failing with `The model "users" was not found in the schema object`, normalized mobile deep-link scheme usage to the dev app scheme, and added missing web auth runtime envs (`APP_URL`, `APP_SCHEME`, `EXPO_PUBLIC_APP_SCHEME`) so the Better Auth server matches Expo's `gradientpeak-dev` scheme.
- Mobile auth init audit: traced startup `Network request failed` errors to server override handling in `apps/mobile/lib/server-config.ts`; custom API overrides were also rewriting the Supabase base URL, so auth bootstrap could fail before routing finished. Fixed by keeping hosted Supabase for non-local overrides, added regression coverage in `apps/mobile/lib/server-config.test.ts`, and clarified the sign-in/sign-up override copy. Validation passed with mobile typecheck and targeted Vitest.
- Discover UX MVP: shipped a lightweight polish pass in `.opencode/specs/discover-ux-mvp/` for Discover browse/search clarity plus linked activity plan, training plan, route, and profile detail screens. Validation passed with mobile typecheck, targeted lint, and targeted Jest.
- Discover UX MVP follow-up: added `apps/mobile/app/(internal)/(tabs)/__tests__/discover-screen.jest.test.tsx` plus a small second-pass visual polish for the Discover type switcher and metadata chips. Validation again passed with mobile typecheck and Discover-focused Jest.
- Discover UX MVP detail-test follow-up: added focused Jest coverage for summary-first detail UX in `apps/mobile/app/(internal)/(standard)/__tests__/activity-plan-detail-scheduling.jest.test.tsx`, `apps/mobile/app/(internal)/(standard)/__tests__/training-plan-deeplink.jest.test.tsx`, `apps/mobile/app/(internal)/(standard)/__tests__/route-detail-screen.jest.test.tsx`, and `apps/mobile/app/(internal)/(standard)/__tests__/user-detail-screen.jest.test.tsx`.
- Archived specs live under `.opencode/specs/archive/`.
- Closed session summaries moved to `.opencode/tasks/archive/2026-03.md`.
