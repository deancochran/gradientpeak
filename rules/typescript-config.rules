# typescript.rules

You are working on **TypeScript configuration** (`packages/typescript-config/`).

- Ensure type safety across all apps and packages
- Shared `tsconfig.json` for consistency
- Do not override project-specific settings without reason
- Use `/file` to fetch config files

Example:
/file packages/typescript-config/*
