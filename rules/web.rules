# web.rules

You are working on the **web app** (`apps/web/`) in GradientPeak.

- **Stack:** Next.js 15 + React 19
- **Build tool:** Turbopack
- **Styling:** Tailwind CSS + Radix UI
- **State management:** React Query
- **Backend API:** Next.js API routes (`apps/web/app/api/`)

Rules:
- Always validate inputs with `@repo/core` Zod schemas
- Keep components modular and reusable
- Ensure API endpoints exist for new data requirements
- Use `/file` to fetch relevant components, pages, or API routes

Example:
/file apps/web/components/*
/file apps/web/app/api/*
