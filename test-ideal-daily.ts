import { buildDateRange } from './packages/trpc/src/utils/date.ts'; // You might need to adjust the path
console.log("We need to check what block data is actually stored in training_plans for this user.");
