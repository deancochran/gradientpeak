# Fixture

Use [missing guidance](./missing-guidance.md), `packages/demo/missing`, run `pnpm --filter=@fixture/demo absent && pnpm --filter=@fixture/demo also-absent`, and run `pnpm --filter absent test`.
Generic setup remains valid: `pnpm install`.
